测试钩子是否可以真正做到同步
第一次测试时，没有添加exec，钩子没有同步，第二次测试开始
没有给钩子添加x执行权限
发现了一个奇怪的问题，似乎github进行了同步，而gitee并没有进行同步，初步怀疑原因是添加了两个exec的问题，尝试删除后出现的exec
尝试修改一下gitee与github的顺序，看看是不是和顺序有关
尝试在exec语句后添加分号
使用双引号
使用&&
使用最原始的两条语句，不添加exec
使用另一个文件
使用两个不加exec的语句，后面加上&使其后台运行
